# Michi Music Player — Python dependencies
# Install: pip install .          (core)
#           pip install .[all]    (all features)

PySide6>=6.7
mutagen>=1.47
numpy>=1.24
PyGObject>=3.46
pycairo>=1.29
dbus-python>=1.3

# ── Optional features (after pip install .) ──
# Recognition: pip install .[recognition]
# shazamio
# pyaudio

# Development: pip install .[dev]
# pytest>=7
# pytest-qt>=4
# ruff

# ── System dependencies (not pip — use your distro package manager) ──
# Debian/Ubuntu:
#   python3-gi gir1.2-gstreamer-1.0 gstreamer1.0-plugins-base
#   gstreamer1.0-plugins-good gstreamer1.0-plugins-bad
#   gstreamer1.0-plugins-ugly gstreamer1.0-libav
# Fedora:
#   python3-gobject gstreamer1-plugins-base gstreamer1-plugins-good
#   gstreamer1-plugins-bad-free gstreamer1-plugins-ugly
# Arch:
#   python-gobject gst-plugins-base gst-plugins-good
#   gst-plugins-bad gst-plugins-ugly gst-libav
